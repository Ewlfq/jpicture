(function ($) {
    'use strict';
    
    $.fn.jp = function (picList) {
        if (typeof picList === 'object') {
            
        } else {
            console.log('Object of type object expected object of type ' + (typeof picList) + ' given.');
        }
        
        return this;
    };
}(jQuery));
