(function ($) {
    'use strict';
    
    $.fn.jp = function (picList) {
        if (typeof yourVariable === 'object') {
            
        } else {
            console.log('');
        }
        
        return this;
    };
}(jQuery));
